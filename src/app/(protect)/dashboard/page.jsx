import Button from "@/components/button";

const page = () => {
  return (
    <>
      <section className="text-right">
        <Button btn="primary" className="text-xl">
          Add Task
        </Button>
      </section>
    </>
  );
};

export default page;
